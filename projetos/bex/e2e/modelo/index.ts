// ── Modelos de dados usados com pb.carregarDados<T>() ────────────────────────
// Adicione aqui cada interface que representa um arquivo de dados (JSON/YAML).

export interface Credenciais {
    matricula: string;
    senha:     string;
}

export interface DadosExemplo {
    usuario: string;
    senha:   string;
}

